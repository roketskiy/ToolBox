<Window x:Class="ToolBox.MissingPathWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="程序路径已失效" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{StaticResource CardBg}" FontFamily="Segoe UI Variable, Segoe UI"
        TextElement.Foreground="{StaticResource TextPrimary}">
    <StackPanel Margin="20">
        <TextBlock Text="程序路径已失效" FontSize="15" FontWeight="SemiBold"/>
        <TextBlock x:Name="PathText" TextWrapping="Wrap" Foreground="{StaticResource TextSecondary}" Margin="0,8,0,0" FontSize="12"/>
        <TextBlock Text="该程序可能已被移动或删除。你可以重新定位程序，或取消本次启动。" TextWrapping="Wrap"
                   Foreground="{StaticResource TextSecondary}" Margin="0,6,0,16" FontSize="12"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" MinWidth="80" Padding="12,4" IsCancel="True" Margin="0,0,8,0"/>
            <Button Content="重新定位" MinWidth="80" Padding="12,4" IsDefault="True" Click="Relocate_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
